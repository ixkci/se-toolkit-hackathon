services:
  backend:
    build: .
    ports:
      - "8000:8000"
    restart: always

  frontend:
    image: nginx:alpine
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    ports:
      - "8080:80"
    restart: always
